server {
    listen   80;
    server_name socialrecipes.com;
    root /app;

    location @rewriteCompositeUi {
        rewrite ^(.*)$ /index.php/$1 last;
    }

    location @rewriteSessionApi {
        rewrite ^(.*)$ /app_dev.php/$1 last;
    }

    location /session/ {
        alias /app/Session/src/Infrastructure/Ui/Http/Symfony;
        index app_dev.php;
        set $subfolder "Session/src/Infrastructure/Ui/Http/Symfony/app_dev.php";
        try_files $uri @rewriteSessionApi;
    }

    location @rewriteRecipesApi {
        rewrite ^(.*)$ /app_dev.php/$1 last;
    }

    location /recipes/ {
        alias /app/Recipes/src/Infrastructure/Ui/Http/Symfony;
        index app_dev.php;
        set $subfolder "Recipes/src/Infrastructure/Ui/Http/Symfony/app.php";
        try_files $uri @rewriteRecipesApi;
    }

    location = /app/CompositeUi/src/Infrastructure/Ui/Http/Symfony/robots.txt {
      allow all;
      log_not_found off;
      access_log off;
    }

    location /app/CompositeUi/src/Infrastructure/Ui/Http/Symfony/images/ {
        autoindex on;
    }

    location /app/CompositeUi/src/Infrastructure/Ui/Http/Symfony/build/ {
        autoindex on;
    }

    location /app/CompositeUi/src/Infrastructure/Ui/Http/Symfony/fonts/ {
        autoindex on;
    }

    location / {
        root /app/CompositeUi/src/Infrastructure/Ui/Http/Symfony;
        index index.php;
        set $subfolder "CompositeUi/src/Infrastructure/Ui/Http/Symfony/index.php";
        try_files $uri @rewriteCompositeUi;
    }
    location ~ ^/(app_dev|index)\.php(/|$) {
        fastcgi_pass php-upstream;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root/$subfolder;
    }
}

